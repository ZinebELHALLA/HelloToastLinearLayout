<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.HelloToast" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.HelloToast" parent="Base.Theme.HelloToast" />

    <style name="CustomButton" parent="Widget.AppCompat.Button">
        <item name="android:background">@color/royal_blue</item>
        <item name="android:textColor">@android:color/white</item> <!-- Optional: Change text color -->
    </style>
</resources>