<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:orientation="vertical"
    android:background="@color/golden_yellow"
    android:padding="0dp">

    <!-- Header Container with royal blue background -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:background="@color/royal_blue"
        android:padding="16dp">

        <!-- Header TextView (aligned to the start/left) -->
        <TextView
            android:id="@+id/header"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:gravity="start"
            android:text="@string/show_toast"
            android:textSize="24sp"
            android:textColor="@color/white"
            android:textStyle="bold"
            android:layout_gravity="start"/>
    </LinearLayout>

    <!-- Toast Button -->
    <Button
        android:id="@+id/button_toast"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/show_toast"
    android:textColor="@color/white"
    android:backgroundTint="@color/royal_blue"
    android:padding="12dp"/>

    <!-- Use weights to center the counter and push the increment button to the bottom -->
    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="0dp"
        android:layout_weight="1"
        android:gravity="center">

        <!-- TextView for the counter -->
        <TextView
            android:id="@+id/textView_count"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:text="@string/count_initial_value"
            android:textSize="64sp"
            android:textColor="@color/royal_blue"
            android:textStyle="bold"
            android:gravity="center"
            android:layout_gravity="center"
            android:paddingTop="32dp"
            android:paddingBottom="32dp"/>
    </LinearLayout>

    <!-- Increment Button at the bottom -->
    <Button
        android:id="@+id/button_increment"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:text="@string/increment"
        android:textColor="@color/white"
        android:backgroundTint="@color/royal_blue"
        android:padding="12dp"
        android:layout_gravity="bottom"/>
</LinearLayout>
